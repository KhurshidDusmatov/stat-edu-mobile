import 'package:flutter/material.dart';

class ProfEducationPage extends StatelessWidget {
  const ProfEducationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Professional ta'lim"),
          backgroundColor: Colors.white,
        ),
    );
  }
}
